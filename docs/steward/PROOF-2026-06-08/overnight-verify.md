# Steward overnight verification

**Run at:** `2026-06-09T14:15:18.723273+00:00`
**Total checks:** 12  **PASS:** 12  **FAIL:** 0  **ERROR:** 0

Generated by `backend/scripts/steward_overnight_verify.py` - the script boots the full Steward router in-process, hits every endpoint shipped during the overnight run (items 1-6), and asserts findings emerge from all 6 active observability levels.

## Checks

| # | Check | Result | Detail |
|---|---|---|---|
| 1 | Archeologist - duplicate_source via /findings | **PASS** | duplicate_source detected across 1 signature(s) |
| 2 | Governance - PUT /governance then env_crossing + unapproved_destination | **PASS** | env_crossing + unapproved_destination both surface from governance policy |
| 3 | Schema-drift - baseline + diff via /schema-snapshot | **PASS** | finding_id=sdrift-ef22ba9365f608be; 2 change(s) detected |
| 4 | Quality - POST /quality-check fans into 3 kinds | **PASS** | 3 quality findings: null_spike (P1) + duplicate_key_spike (P1) + quality_check_failed (P2) |
| 5 | Connector-health - 3 failures + time-clamp bypass -> finding | **PASS** | 3 auth failures + back-dated streak -> connector_auth_failure surfaces |
| 6 | Cost - 3 zero-output runs -> warehouse_waste | **PASS** | 3 zero-output runs on same source -> warehouse_waste (P2) |
| 7 | Node - 3 zero-output node runs -> empty_output | **PASS** | 3 zero-output runs on same (workflow, node) -> empty_output (P2) |
| 8 | User rules - YAML on disk -> findings via /rules + /findings | **PASS** | YAML rule loaded; 1 match(es) on wf-A |
| 9 | Resolve -> PROPOSED lesson via fix_note | **PASS** | PROPOSED lesson 5078b586e376... captured from fix_note |
| 10 | Lesson approve + search (gated learning Rule 3) | **PASS** | lesson approved by verify-script; search returned 0 hit(s) |
| 11 | Memory journal - file exists and JSONL parses | **PASS** | journal has 88 parseable JSONL events |
| 12 | Cross-cutting - all 6 active levels surfaced this run | **PASS** | levels surfaced across run (journal+current): ['architecture', 'connector', 'cost', 'data', 'governance', 'node', 'pipeline'] |

## What was exercised

| Endpoint | Pinned by | Outcome |
|---|---|---|
| `GET /api/steward/findings` | every step | Findings render across 6 active levels |
| `PUT /api/steward/governance` | step 2 | env_tags + approved_destinations persisted, detector fires |
| `POST /api/steward/schema-snapshot` | step 3 | baseline + diff -> finding |
| `POST /api/steward/quality-check` | step 4 | failed assertions fan into 3 FindingKinds |
| `POST /api/steward/connector-health` | step 5 | streak detection + time-clamp behaviour |
| `POST /api/steward/cost-event` (source) | step 6 | warehouse_waste streak |
| `POST /api/steward/cost-event` (node) | step 7 | empty_output streak |
| `GET /api/steward/rules` + filesystem YAML | step 8 | rule load + finding emission |
| `POST /api/steward/findings/{id}/resolve` | step 9 | fix_note -> PROPOSED lesson |
| `POST /api/steward/lessons/{id}/approve` | step 10 | gated learning (Rule 3) |
| memory journal on disk | step 11 | emit + resolve events recorded |

## What this does NOT cover

- **Frontend rendering** of the new FindingKinds - needs a separate in-browser session (verify the eye-icon dropdown, level filters, per-kind icons / colours land correctly for governance / cost / node findings)
- **Live infrastructure** - no real PostgreSQL was probed; the PostgreSQL connector deepening (item 4) needs a separate live-DB run via `python -m fpulse.scripts.postgres_smoke_test --dsn ...`
- **Notification bell + email fan-out** - the notifier code path is stubbed (no notification_store in this isolated app); pinning that needs the full app startup
- **Persistence-occurrence escalation across multiple scans** - the time-clamp / escalation behaviour is unit-tested but not exercised longitudinally here

## How to re-run

```powershell
# from the repo root:
$env:PYTHONPATH = "backend"
.venv\Scripts\python.exe -m fpulse.scripts.steward_overnight_verify
```

Exit 0 means every check passed. Exit 1 means at least one check FAILed or ERRORed - see the table above.
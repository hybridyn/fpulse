import FPulseNode from './FPulseNode';
import CustomEdge from './CustomEdge';
import StickyNoteNode from './StickyNote';

export const nodeTypes = {
  fpulseNode: FPulseNode,
  stickyNote: StickyNoteNode,
};

export const edgeTypes = {
  custom: CustomEdge,
};

/**
 * @deprecated removed May 10 2026 — replaced by HubTabs, which renders
 * the centered tab strip INSIDE each child page's existing 78px banner
 * instead of wrapping the page in a separate hub header. The pattern
 * now matches Insights and Settings exactly: one page header per page,
 * with title (per-page), tabs (centered), and actions (per-page) all
 * in the same row.
 *
 * Kept as a stub so cached build artifacts referencing the old import
 * path don't error at module-load time. Delete after the 1.0.1 cleanup.
 */
export {};

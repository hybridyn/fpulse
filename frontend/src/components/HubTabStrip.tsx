/**
 * @deprecated removed May 9 2026 — replaced by HubHeader, which renders
 * the tabs INSIDE the canonical 78px page banner (matching Insights +
 * Settings) instead of as a separate strip above the page chrome.
 *
 * This file remains as a stub so any cached build artifacts referencing
 * the old import path don't error at module-load time. Delete after the
 * 1.0.1 cleanup pass.
 */
export {};
